import React from 'react';

const UserMenu = ({ userEmail, onLogout }) => {
  return (
    <div>
      <p>mango@mail.com</p>
      <button>Logout</button>
    </div>
  );
};

export default UserMenu;
